{
  "name": "careerpath",
  "version": "0.1.0",
  "private": true,
  "description": "CareerPath: pick a target role, learn the skills it needs, and prove them through assessment.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "serve": "node test/devserver.mjs",
    "mock-provider": "node test/fakeopenai.mjs",
    "test": "node test/app.spec.mjs && node test/assistant.spec.mjs && node test/deploy.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
